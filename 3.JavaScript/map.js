let arr = [1,2,3,4,5,6];
let newArr = arr.map((value,index)=>(value > 2));
// console.log(arr);
// console.log(newArr);

// filter function

const ages = [32, 33, 12, 40];
ages.filter(function(value, index){
    if(value > 18){
        // console.log(value);
    }
});

//reduce function
const numbers = [175, 50, 25];
numbers.reduce(function(value, index){
    // console.log(value)
});
